{
  "compilerOptions": {
    "baseUrl": "./tests", // Установите baseUrl на tests
    "paths": {
      "*": ["*"], // Это позволит вам импортировать файлы из tests/pages и других папок
      "@pages/*": ["pages/*"] // Добавьте алиас для папки pages
    }
  }
}